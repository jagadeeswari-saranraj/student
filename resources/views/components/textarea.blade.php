<div>
    <div class="form-group">
        <label for="{{trim($title)}}">{{$title}}</label>
        <textarea class="form-control" name="{{$name}}" id="{{trim($title)}}"> {{$description}} </textarea>
    </div>
</div>