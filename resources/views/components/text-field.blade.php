<div>
    <div class="form-group">
        <label for="{{trim($title)}}">{{$title}}</label>
        <input type="text" class="form-control" name="{{$name}}" id="{{trim($title)}}" value="{{$value}}" placeholder="{{$title}}">
  </div>
</div>